#Mi url de backend
NEXT_PUBLIC_API_BASE=http://localhost:3001
#~~~~~~~

#Auth0 
#lo que usa auth0 para encriptar la cookie
AUTH0_SECRET='' 
#Mi url de front
AUTH0_BASE_URL='http://localhost:3000' 
#url que usa auth0 para hacer login/logout de usuarios (el domain de la app)
AUTH0_ISSUER_BASE_URL='' 
#id  que tenemos en auth0 (el de la application)
AUTH0_CLIENT_ID=''
#secret que tenemos en auth0 (el de la application)
AUTH0_CLIENT_SECRET=''
#Es el identifier de la API generada en auth0
AUTH0_AUDIENCE=''